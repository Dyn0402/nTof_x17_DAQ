# Liquid scintillator (LIQ/LS) calibrations — placeholder

No data yet: only LIQA/LIQD were cabled in July and delivered no readout
(see run224404 collaboration requests). Expected content once LS channels read
out:

- MIP calibration via wall × plastic × LS triple coincidence (also provides
  the true plastic MIP calibration — see `../pss/README.md`).
- PSD (pulse-shape discrimination) parameters if used for n/γ separation.

One LS layer per arm (the second layer was removed from the stack, 2026-07-15;
see MX17_Full_Geant geometry docs).
