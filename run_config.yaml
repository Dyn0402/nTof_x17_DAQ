# DAQ Configuration File
experiment_name: "DREAM_Test_Beam_2026"
version: 1.0

dream_daq_info:
  daq_config_template_path: "/path/to/templates/standard_cfg.txt"
  run_directory: "/mnt/daq/runs"
  data_out_dir: "/data/output/raw"
  raw_daq_inner_dir: "inner_raw"
  copy_on_fly: true
  zero_suppress: false           # Optional: defaults to False in code
  n_samples_per_waveform: 1024   # Optional: defaults to None in code
  pedestals_dir: "/data/calib/pedestals"
  pedestals: "pedestal_run_001.root"